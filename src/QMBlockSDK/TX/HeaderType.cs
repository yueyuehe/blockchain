namespace QMBlockSDK.TX
{
    public enum HeaderType
    {
        MESSAGE,
        CONFIG,
        CONFIG_UPDATE,
        ENDORSER_TRANSACTION,
        ORDERER_TRANSACTION,
        DELIVER_SEEK_INFO,
        CHAINCODE_PACKAGE,
    }
}
